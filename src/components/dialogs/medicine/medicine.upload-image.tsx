// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface MedicineUploadImageProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  medicineId: string;
}

export function MedicineUploadImage() {
  return (
    <div>
      <h1>Medicine Upload Image</h1>
    </div>
  );
}